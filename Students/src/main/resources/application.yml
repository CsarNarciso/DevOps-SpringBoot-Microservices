server:
  port: 8001

spring:
  application:
    name: msvc-Students
  datasource:
    url: jdbc:postgresql://localhost:5432/students
    username: postgres
    password: 123
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
